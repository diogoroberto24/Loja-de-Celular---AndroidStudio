<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifpr.lojadecelularessqlite">

    <application
        android:allowBackup="true"
        android:label="Loja de Celulares"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".FormCompraActivity" />
        <activity android:name=".ComprasActivity" />
        <activity android:name=".FormAparelhoActivity" />
        <activity android:name=".AparelhosActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>